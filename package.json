{
  "name": "sponsorship-intelligence-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-backend": "cd sponsorship-backend && npm install",
    "build": "cd sponsorship-backend && npm install && npx prisma generate",
    "start": "cd sponsorship-backend && npx prisma migrate deploy && node index.js"
  }
}